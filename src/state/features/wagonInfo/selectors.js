export const selectWagonFullName = (state) => {
    return state.wagonInfo.wagonFullName;
}

export const selectIsSubmitButtonWagonNameClicked = (state) => {
    return state.wagonInfo.isSubmitButtonClicked;
}